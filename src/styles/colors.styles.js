export const lightBlue = '#2f95dc';

const tintColor = '#2f95dc';
const lightColor = '#fff';
const greyColor = '#ccc';

export default {
	baseBackground: lightColor,
	tabIconDefault: greyColor,
	tabIconSelected: tintColor
};
